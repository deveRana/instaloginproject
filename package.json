{
  "name": "serverside",
  "version": "1.0.0",
  "description": "",
  "engines": {
    "node": "15.x",
    "npm": "6.14.4"
  },
  "main": "index.js",
  "scripts": {
    "start": "node app.js",
    "heroku-postbuild": "cd client && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongoose": "^5.13.2"
  }
}
